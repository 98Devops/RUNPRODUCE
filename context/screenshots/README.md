# Reference screenshots

Harvested with Playwright MCP. See `../ui-reference-playbook.md`.

Naming: `reference-<source>-<view>-<width>.png`
e.g. `reference-mercury-cashflow-1440.png`

These are **research artefacts for pattern extraction**, not assets to
copy. No reference imagery, icon, or brand mark ever ships in the
product.

Also keep `runproduce-<screen>-<width>.png` here during UI units, so the
audit step can compare our output against the references directly.
